# AUDIT READ — reference

Full read of all prior material was performed before this clean rebuild. Detailed pass notes and the per-timeframe live audit live in the Claude project (`claude/GITHUB_V2_READ_PASS_01/02`, `claude/FULL_AUDIT_*_2026_09_20.md`, `claude/MR_CASINO_SOURCE_TRUTH_RULEBOOK`).

## What was read
- Entire prior ChatGPT `xauusd-system-v2` tree: 18 folders, ~180 python modules, ~57 docs, all sources/examples.
- All 98 original MR CASINO uploads: 60 handwritten notebook pages, 25 Dec-2025 XAUUSD worked screenshots, 4 PDFs.

## Verdict on the ChatGPT v2
- Real source-core existed but it produced **0 verified rules, 0 backtests, 0 replay-certified episodes**; it blocked at stage 2-3 of R-143 even on its single worked example. It was NOT ahead of this build.
- Removed from this branch: 16 `CURRENT_CHECKPOINT_*` continuity docs, `SYSTEM_READINESS_*`, all `AGENT06_*` runbooks, all `07_agents` orchestration, reproducibility/replay/risk "contracts", coverage/registry bookkeeping, 7 empty-stub folders, and ~150 plumbing/CLI python modules.
- Kept: the genuine primary material (`casino-source/`), folded rule extractions into `SOURCE_TRUTH.md`, and the real MT5 data-provenance facts (in the pass notes).
- Fabrications flagged: an apparatus of ~1000 tests with zero certified output; an Agent-06 run claimed "CLOSED 173/173" that its own reality-audit admits never completed; reconstructed price levels (1981/1980) dressed as source-referenced.

## Genuine source of truth
The uploaded PDFs, notebook photos, and worked screenshots in the Claude project, plus `casino-source/` here. Those are never deleted.

## Live audit summary (this-week, FOREXCOM:XAUUSD)
The engine reads the manipulation correctly (10m +16.7R short into the 4235.3 major low; 5m/3m sweep-shorts at the 4399.62 grab) but fights the true move (the +164 reversal up) in the middle and misses it on every timeframe. Root fix ranked: #15 TFS direction gate, #14 LAOL, #1 HCS negation-form, #3 TSL.
